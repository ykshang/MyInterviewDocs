# Express

## 一、概述

**概念**：`Express` 是基于 `Node.js` 平台，快速、开发、极简的 Web 开发框架。  
**通俗理解**：`Express` 的作用和 `Node.js` 内置的 `http` 模块类似， 是 `http` 模块的**二次封装**，提供了创建 Web 服务器的便捷方式。  
**Express 的中文官网**：[http://www.expressjs.com.cn/](http://www.expressjs.com.cn/)

对于前端程序员来说，使用 Express，我们可以方便、快速的创建这两种常见服务器：

- **Web 网站服务器**：专门对外提供 Web 网页资源的服务器。
- **API 接口服务器**：专门对外提供 API 接口的服务器。
